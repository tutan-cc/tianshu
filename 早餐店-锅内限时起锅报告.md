# 早餐店 · 锅内「限时起锅」改造报告（bf-14）

> 需求原话：**「餐盘可以一直放着，锅里也可以同时煮着，但是锅里的熟了必须在规定时间内起锅啊，
> 如果备用餐盘里放着，锅里要起锅的没地方放就只能糊掉，然后想再使用锅，就要双击扔掉里面的食物」**

一句话结论：**盘子继续永久保鲜；锅变成唯一的压力点 —— 锅里一到「恰好」不再自动落盘，
而是开一个 `SERVE_WINDOW`（4.5s）的「起锅窗口」（火候暂停 + 倒计时环），窗口内**单击锅**才起锅落盘；
盘里已经有一份 → 没地方放 → 提示「盘里还有一份，先送出去」并**继续计时** → 走完那份就糊；
糊了这口锅被占死（下料被拒），**必须双击锅（或该列盘）丢掉**才能再用，丢弃不扣分。**

---

## 1. 状态机（两条路径，互不影响）

### 1.1 锅（唯一压力点 · bf-14 新增）

```
idle ──点食材──▶ raw ──(t ≥ dur)──▶ perfect ──┬── 窗口内单击锅 ──▶ 盘（该列专属盘）
                                              │      takeOut()       桩位立刻空出来
                                              └── 窗口走完没起锅 ──▶ burnt（糊 · 占死这口锅）
                                                    burnStation()        st.burnt++ / st.expire++
                                                                               │
                                        burnt ── 双击锅 / 该列盘 ──▶ idle     │
                                                  trashStation()（不扣分）◀────┘
```

| 阶段 | 触发 | 火候 | 锅上文案 | 备注 |
|---|---|---|---|---|
| `raw` | `placeFoodEx()` 成功 | `t += dt` 累加 | `生 N.Ns` | 9 种下锅音效在这一刻响（一个字没改） |
| `perfect` | `t` 跨过 `dur` | **冻结**（`isServeWindowOpen` → 不再累加 `t`） | `起锅！点一下这口锅` + `恰好 · 起锅 N.Ns` | 起锅窗口 = `SERVE_WINDOW`(4.5s)，橙色倒计时环，最后一秒红闪 |
| 起锅成功 | 窗口内单击锅且**该列盘空** | — | `起锅 · 落到X盘（热乎）` 飘字 | `st.prepped++`；盘上从此永久保鲜 |
| 起锅被拒 | 窗口内单击锅但**该列盘被占** | 不变（不重置、不暂停） | `盘占着 · 没地方放`（红） | 原因码 `plate-occupied`；窗口继续走 |
| `burnt` | 窗口走完还没起锅 | 冻结 | `不能再下料 · 双击丢` + `糊了 · 双击丢掉` | 焦黑锅体 + 红叉 + 冒烟；`st.burnt++`、`st.expire++` |
| 清锅 | **双击锅 / 该列盘** | — | `丢掉糊的（不扣分）` | 唯一出口；**不再有 14 秒自动清** |

> 「窗口内暂停火候」是必要的：完美窗口 `pw` 只有 0.6~1.0s，若不按暂停，`SERVE_WINDOW` 会被 `pw` 提前吃掉，
> 「规定时间内起锅」会退化成「1 秒内起锅」。沿用旧 `openServeWindow` 的同一手法，只是这次把它接到了**每一列**上。
>
> **如实记录**：因此默认玩法下**「过火」这一档不再出现**（恰好一到就定格等人起锅）。
> `cookState()` 的四段曲线**一个字节都没改**，「按住看火」（`manual` / `setCook` / `debug.place`）那条路
> 照样能完整看到 生 → 恰好 → 过火 → 糊，单测仍在逐条验它（`火候状态机` 两条用例 + 缺口补齐）。

### 1.2 盘（bf-13 永久保鲜 · 一个字没改）

```
（锅起锅）──▶ plate：tier 冻结在落盘那一刻（完美 → 热乎 14 分）· left = Infinity（没有倒计时）
                    不会凉（不降档）· 不会糊（无 burnt 分支）· 不会自己消失
      单击盘 ──▶ 送给「正需要这份 + 耐心最少」的顾客（三级排序没动）
      双击盘 ──▶ 丢垃圾桶（不扣分）
      盘空 + 锅空 ──▶ 这一列可再下料
```

**一处与 bf-13 的口径差异（有意、且是规则 3 能成立的前提）**：盘里有一份时**允许继续下料**
（用户原话「餐盘可以一直放着，锅里也可以同时煮着」）。原因码 `plate-occupied` 因此从「**下料**被拒」
搬到了「**起锅**被拒」—— 不放开这一条，「盘被占 → 没地方放 → 糊」这条规则永远触发不到
（旧口径下盘占着就连料都下不去）。`stationFree()` 也随之改成**只看锅**。

---

## 2. 交互表（写进 UI 的就是这张表）

| 目标 | 单击 | 双击 | 单击被拒时 |
|---|---|---|---|
| **锅内成品**（`perfect`，窗口开着） | **起锅** → 落进该列专属盘（盘空） | 丢垃圾桶（清锅 + 清盘，不扣分） | 盘被占 → `盘里还有一份，先送出去` + 剩余秒数提示；窗口**继续走** |
| **锅内**（`raw`） | 提示 `锅里还在做 —— X 要 N.Ns` | 丢垃圾桶（倒掉生料） | — |
| **锅内**（`burnt`） | 提示 `糊了，只能丢掉（双击这一列）` | **丢掉糊的** → 这口锅恢复可用 | 下料被拒 `station-occupied` |
| **专属盘**（有食物） | **送客** → 自动挑「正需要 + 耐心最少」那位 | 丢垃圾桶（不扣分） | 没人要 → `no-want`，留在盘上（永久保鲜） |
| **专属盘**（空） | 提示 `盘里还空着 —— 锅里那份正等着起锅：点一下锅（还剩 N.Ns）` | 无事发生 | — |
| **食材桶** | 下料（进它自己那一列） | — | `station-occupied` / `wrong-column` |
| **顾客卡** | 用最合适的一盘上菜（旧操作保留） | — | — |

> 单击热区、拖拽下料、`DOUBLE_MS = 425ms` 双击判定、9 列列对齐 —— 全部保持不变。

---

## 3. 新常量与现值

| 常量 | 值 | 语义 | 位置 |
|---|---|---|---|
| **`SERVE_WINDOW`** | **4.5**（值未改，**语义变了**） | 锅内**起锅窗口**秒数（原：「兼容层 / 盘上过火计时」） | 170 |
| `SERVE_WARN_SEC` | 1.0 | 窗口最后一秒 → 倒计时环**红闪** | 171 |
| `PAN_BURNT_STICKY` | `true` | 糊锅**只能靠双击清**（不自动清、不过期） | 180 |
| `PAN_PICK_TEXT` | `"恰好 · 起锅"` | 窗口内锅底那行字（UI 与规则同源） | 183 |
| `PAN_BLOCKED_TEXT` | `"盘占着 · 没地方放"` | 盘被占时的红字 | 184 |
| `PAN_BURNT_TEXT` | `"糊了 · 双击丢掉"` | 糊锅那行字 | 185 |
| `PLATE_KEEP` / `PLATE_NO_TIMER` / `PLATE_KEEP_TEXT` | `true` / `Infinity` / `"∞ 可一直放着"` | 盘上永久保鲜（bf-13 原样） | 191 / 192 / 1894 |
| `BURNT_LIFE_MS` | 14000（**仅兼容保留**） | bf-13 及以前「锅内残骸 14 秒自动清」；bf-14 起 `step()` **不再读它** | 181 |

**关于 `SERVE_WINDOW` 的现值与建议值（用户要求说明）**：
* **现值 = 4.5s**，不是需求描述里的 3.5s（3.5s 是 `PARTIAL_PATIENCE_BONUS`，两回事）。这一轮**没有改这个数**。
* **建议保持 4.5s**，理由三条：
  1. 窗口内火候是**暂停**的，4.5s 是玩家真正拥有的秒数；9 口锅并行时，一次「看单 → 点锅 → 点盘」大约 1.5~2.5s，
     4.5s 允许玩家同时照顾 2~3 口锅，压力刚好落在「手快有、手慢糊」而不是「必糊」；
  2. 收短到 3.0s 以下时，一局 75s 里「边做边送」会变成纯手速赛，`st.expire` 会显著上升（本报告的探针可复现）；
  3. 3 处既有断言 / 无头验收 / 出图脚本都锚定了 4.5，改数会放大回归面。
* 想调，**只改这一个常量**（170 行）；`SERVE_WARN_SEC` 决定最后一秒红闪，二者独立。

---

## 4. 断言改动逐条对照（原 → 新 → 原因）

> 纪律：**一条断言都没有删除**，只按新口径改写 + 新增。净增断言量与用例数见 §5。

### 4.1 `tests/breakfast.test.cjs`（86 → **90** 例，assert 调用 1076 → **1255**）

| # | 用例 | 原期望 | 新期望 | 原因 |
|---|---|---|---|---|
| 1 | 计分…（「自动落盘：真实帧推进下」段） | `advance(dur+0.1)` → `st.stations[3].food === null`、盘上有蛋、`phaseOf="plated"` | 同样的推进 → `state="perfect"`、`plates.length === 0`、`takeOut(3)` 之后才空锅/落盘、`phaseOf` 走 `window → plated` | 熟了不再自动落盘（本轮核心口径） |
| 2 | 点击下料路由… | 盘占着 → `placeFoodEx().why === "plate-occupied"`（下料被拒） | 盘占着**下料成功**（`ok===true`）；`takeOut(3)` → `why==="plate-occupied"`（起锅被拒）+ 提示 `/盘里还有一份/` + `left > 0`（窗口还在走）；盘一空再点锅即成功 | 原因码搬家：从下料搬到起锅；这是「盘被占 → 糊」的前提 |
| 3 | 每一列都有专属盘… | `advance(5.2)` → 9 盘自动落满；`stationFree===false` + `plate-occupied` | 逐列 `pickOut()`（窗口内点锅）→ 9 盘；`stationFree===true` + 盘占着也能下料（验完立刻还原成 9 盘） | 同上；`stationFree` 语义改成只看锅 |
| 4 | 熟了自动落到本列专属盘（autoPlate 默认开）…（整条） | `cfg.autoPlate === true`、熟了自动落盘、`autoPlate:false` 才手动 | 用例改名 **bf-14：autoPlate 默认关闭**：`false`（不传）/ `false`（显式 false）/ `true`（显式 true 才回旧口径）；默认路径验 `window` 状态机 + `takeOut` 落盘；manual 验 `serveWin===0`（不开窗口）→ `over` → `burnt` | 默认值翻转 + 窗口把 `over` 挤出手动路之外的默认路径 |
| 5 | 盘上永久保鲜：煮得久的菜… | `advance(dur+0.1)` → 落盘；`p.age ≤ 0.25` | `pickOut(0)` → 落盘；`p.age` 从 `mkPlate` 起就是 0（修掉 `undefined`） | 落盘方式变了；`age` 补初始化 |
| 6 | 永久保鲜②：不糊 / 不冒烟 / 不消失 / 不堵死 | `placeFoodEx("bun").why === "plate-occupied"` | 下料成功 → `takeOut` 被拒（`plate-occupied`）→ 盘上那份没被动过 → 清列后重做一份放回盘上（原「双击仍丢得掉 / 丢完能再用」逐条保留） | 同 #2 |
| 7 | 永久保鲜③ ③b | `autoPlate === true`、熟了立刻落盘、60s 不糊 | `autoPlate === false`、`pickOut` 落盘、盘上 60s 后仍 `hot` | 同 #4；「锅内压力不回流到盘上」这条结论不变 |
| 8 | 下锅音效④：9 样食材各有各的文件 | `advance(2.6)` → 自动落盘 | `pickOut(3, 6)` → 起锅落盘（音效口径不变：只在下锅那一刻响） | 落盘方式变了 |
| 9 | 下锅音效④：没有 Audio 的环境… | `advance(dur+0.1)` → 自动落盘 | `pickOut(8, 6)` → 起锅落盘 | 同上 |
| 10 | helper `plateState()` | `autoPlate:true`（旧默认） | **不传 autoPlate**（页面口径） | 让整套测试跑在新口径上 |
| 11 | helper `autoServe()` | 下锅后等自动落盘 | 下锅 → `pickOut()` → 单击盘；盘被占先送出去 | 同上 |
| 12 | helper `botRun()` / 过关判定机器人 | 等 `plates()` 出现 | ① 起锅（窗口内点锅 / 盘占先送）② 盘上存货送出去 ③ 看单下料 | 同上 |
| 13 | 新增 `pickOut()` / `advToWindow()` | — | 窗口内单击锅的确定性封装（推进 → `takeOut`） | 新交互的测试原语 |

### 4.2 `tools/bf/headless.js`（486 → **526** 条 check，`A()` 调用 420 → 461）

| # | 段 | 原期望 | 新期望 | 原因 |
|---|---|---|---|---|
| 1 | ⑤ 真实鼠标 | tick 到盘上有蛋 → `plate` | tick 到 `windowOpen` → 断言盘上**空** → **真实 mousedown 点锅** → `plate === "egg"` + `tier === "hot"` | 新交互的主证据（走 `onDown` 真事件） |
| 2 | ⑤ 再做一份 | 等自动落盘 | 等窗口 → `takeOut(3)` | 同上 |
| 3 | ⑥.5 常量 | `autoPlate === true` | `autoPlate === false` + `panWindow===4.5` / `panBurntSticky===true` / `SERVE_WARN_SEC===1.0` + 三句文案常量 | 默认翻转 + 新常量 |
| 4 | ⑥.5 落盘 | 自动落到第 0 列 | 熟了**没落盘**、`phase="window"`、`takeOut(0)` → 落盘 | 同上 |
| 5 | ⑥.5 盘占用 | 下料被拒 `plate-occupied` | 下料**成功**；`takeOut` 被拒 `plate-occupied` + 提示 + 窗口继续；出餐后**锅里那份不许被清掉**（真机修复点）；`takeOut` 成功；**重新下一份 → 不点 → 糊**（`burnt=1/expire=1`）→ 20s 后**仍在**（不自动清）→ 双击才清、不扣分、能再用 | 这是本轮规则的端到端证据 |
| 6 | ⑥.5 没人要那份 | tick 到盘上 | tick 到窗口 → `takeOut` | 落盘方式 |
| 7 | ⑥.5 `expire` 断言 | `expire === 0`（绝对值） | `expire === exp0`（相对值，盘龄拨表不许动这个账；忘起锅只由锅内窗口产生） | 上面刚制造过一次忘起锅 |
| 8 | ⑥.6 | 自动落盘 / 盘占着下料被拒 | 窗口 + `takeOut`；盘占着**也能下料**、`takeOut` 给 `raw` | 同 4.1 #2 |
| 9 | ⑥.7 渲染 | 盘上画「热乎 / ∞ 可一直放着 / 不画糊」 | 原 4 条**保留**并改精确（`/煎蛋·糊了/`、`/只能丢/`）；**新增 6 条**：锅内画「起锅！点一下这口锅」、`/恰好 · 起锅 \d+\.\d+s/`、**橙色倒计时环**（`#ffb347` strokes 增长）、图例「熟了 4.5s 内点锅起锅」、底排「盘被占了就只能糊掉」、**画面上不存在「自动落盘 / 内送出」旧文案** | UI 必须与行为一致 |
| 10 | ⑦ 通过路径机器人 | 等自动落盘 | 起锅 pass + 送餐 pass + 下料 pass；**新增 1 条**：`win.burnt === 0`（新规则下真能零糊通关） | 同上 |
| 11 | ⑧ `driveWin()` | 等自动落盘 | 同 ⑦ 三段式 | 把「胶水层真打赢一局」这条证据保住 |
| 12 | 音效段 | 等自动落盘 | 窗口 → `takeOut` | 同上 |
| 13 | bf-9 段 ×3 | 等自动落盘 | 窗口 → `takeOut` | 同上 |
| 14 | 火候三态描边 / 糊飘字 / 糊盘红叉 | — | **原样保留**（都走 `manual` 或 `burnPlate`，不受影响） | 锅内曲线回归 |

### 4.3 `tools/bf/e2e-audio.js`（真 Chrome，31 → **32**）

| # | 段 | 原 | 新 | 原因 |
|---|---|---|---|---|
| 1 | ⑤ 上餐成功 → 欢呼 机器人 | 等自动落盘 → 点盘 | 窗口内**先点锅起锅**（盘占先点盘）→ 点盘 | 新口径 |
| 2 | ⑦ 上餐选人 | 等自动落盘 → 点盘 | 游戏时钟推到窗口 → **真实 MouseEvent 单击锅** → 点盘；**新增 1 条断言**「真实鼠标单击锅 → 起锅入盘（窗口剩余 4.5s · tier=hot）」 | 新交互在真浏览器里的直接证据 |

### 4.4 出图脚本 `tools/bf/shots/panel.js`

| # | 变化 |
|---|---|
| 1 | `bot()` 改成三段式（起锅 / 送餐 / 下料） |
| 2 | `bf_game.png`：5 盘保鲜（盘龄 0.4~90s）+ **2 口锅在窗口里**（起锅提示 + 倒计时）+ **1 口已糊**（焦黑 + 红叉） |
| 3 | `bf_columns.png`：三列 = 等在窗口 / 刚起锅落盘 / 还在烧；底部操作说明按新口径改写 |
| 4 | `bf_plates_nocountdown.png`：9 份改成「窗口内点锅起锅」落盘；代码口径那行改写 |
| 5 | **新增 `bf_pan_window.png`**（本轮主图，见 §6） |
| 6 | 结算面板摹本「忘取」→「忘起锅」 |

---

## 5. 全部测试数字（**本次真跑**）

| 验收项 | 命令 | 基线 | 改后 | 结果 |
|---|---|---|---|---|
| 语法 | `node --check breakfast.js` | exit 0 | **exit 0** | ✔ |
| 内联语法 | `node tools/dev/check-inline.js` | 2 段 / 失败 0 | **2 段 / 失败 0** | ✔ |
| 纯逻辑单测 | `node tests/breakfast.test.cjs` | 86 / 86 | **90 / 90**（+4 例） | ✔ 0 失败 |
| 无头渲染验收 | `node tools/bf/headless.js` | 486 / 486 | **526 / 526** | ✔ 0 失败 |
| 真 Chrome 验收 | `node tools/bf/e2e-audio.js` | 31 / 31 | **32 / 32**（+1 条：真实鼠标单击锅） | ✔ 0 失败 |
| 压力探针（本轮新增） | `node tools/bf/_patch/_bf14_pressure_probe.js` | —（新） | **63 / 63** | ✔ |
| 音频清单（顺带回归） | `node tests/audio-paths.test.cjs` | 20 / 20 | **20 / 20** | ✔ |
| 出图 | `node tools/bf/shots/panel.js` | 6 张 | **7 张**（含新图 bf_pan_window.png） | ✔ |

断言量（脚本统计）：`tests` 的 `assert.` **1076 → 1255（净 +179）**，用例 **86 → 90**；
`headless` 的 `A()` **420 → 461（净 +41）**、check **486 → 526** —— **没有任何断言被删除**。

### 5.1 压力探针（验收第 3 条 · 真跑输出）

`node tools/bf/_patch/_bf14_pressure_probe.js`：

```
══ 探针 A：点完 9 样食材 → 什么都不做 60 秒 ══
  锅上出现过的状态帧：raw 1311 / perfect 2439 / burnt 28650  ← 注意：没有 over 帧（见 §1.1 的如实记录）
  ✔ 不传 autoPlate 时默认关闭（autoPlate=false）
  ✔ 9 口锅同时开工（多锅并行不受影响）
  ✔ 9 份全部在起锅窗口超时后糊掉（st.burnt = 9）
  ✔ 记了 9 次「忘起锅」（st.expire = 9）
  ✔ 盘上 0 份（没有任何一份自动落盘）
  ✔ 9 口锅 60s 后**仍然糊着**（糊锅不会自动清 —— 必须双击）
  ✔ 糊着的锅不能再下料（station-occupied）→ 双击第 3 列 → 锅清空、不扣分、马上能再用
══ 探针 B：盘里放着 → 锅里没地方起锅 → 只能糊 ══
  ✔ 起锅进盘 → 盘里还放着一份时**照样能下料** → 窗口内点锅被拒（plate-occupied + 提示正确）
  ✔ 窗口内还剩 0.01s：还没糊（窗口没被拒绝重置）→ 走完**糊了**（burnt=1/expire=1）
  ✔ 盘上第一份完好无损 → 糊锅不能再下料 → 双击丢弃 → 不扣分 → 恢复可用
══ 探针 C：窗口边界 / 多锅并行 / 口径自检 ══
  ✔ 窗口 4.48s（SERVE_WINDOW-ε）**不糊** / 4.52s（+ε）**糊**（确定性时钟推进）
  ✔ 3 口锅**同时在**窗口里（2.70 / 3.48 / 4.50s）→ 起锅一口，另两口照常倒数、互不干扰
  ✔ 显式 autoPlate:true 仍可打开 · 不传 → 关闭 · 盘上 60s 仍 perfect/hot、left=Infinity
```

**页面路径证据（「不传该参数也关闭」的最后一道证据链）**：
`index.html` 里**唯一**的开局调用点（第 4893 行）传的是
`{ target, duration: 75, goal: 8, practice, onFinish }` —— **没有 `autoPlate`**，
全文搜索 `autoPlate` 命中 **0 处** ⇒ 玩家那一侧永远走「限时起锅」。
（这一处我**没有改动** `index.html`：证据来自对当前磁盘版本的阅读，会话期间它被其他人改过一处，
`check-inline` 对它当前版本的 2 段内联脚本检查仍然通过。）

### 5.2 真 Chrome 里的新交互证据

```
✔ 真浏览器：真实鼠标**单击锅** → 起锅落到本列专属盘（bf-14 新交互；入盘即最高档「热乎」）
   [窗口剩余 4.5s · 盘上 tier=hot]
```

---

## 6. 出图

| 文件 | 说明 |
|---|---|
| **`测试截图/bf_pan_window.png`**（新，2596×1348） | **本轮主图**。同一帧三种锅内状态：**第 3 列煎蛋 = 起锅窗口**（绿环 +「起锅！点一下这口锅」+「恰好 · 起锅 3.1s」倒计时 + 橙色窗口环）· **第 4 列培根 = 没人起锅已糊**（焦黑锅体 + 红叉 +「不能再下料 · 双击丢」+「糊了 · 双击丢掉」）· **第 5 列三明治 = 盘占着没地方放**（盘上「三明治·热乎 / ∞ 可一直放着」，锅上红字「盘占着 · 没地方放」）。底部注解带写清**交互表**（单击锅 = 起锅 / 单击盘 = 送客 / 双击 = 丢弃）、**后果**（盘被占 → 起锅被拒 → 窗口继续走 → 糊；糊锅必须双击才清、残骸不再自动消失）、**盘上永久保鲜保持不变**，并打出**本帧实测计数**：「等着起锅 1 口（煎蛋剩 3.1s）· 已糊 1 口（burnt=1）· 盘占着没地方放 1 口 · 盘上保鲜 1 份」。 |
| `测试截图/bf_game.png`（更新，1770×1185） | 进行中整屏：顶栏明细改成「盘上 5/9 · **锅里等着起锅 2 份**」+「糊 1（**忘起锅** 1）」；图例 ②「熟了 4.5s 内点锅起锅」、④「双击盘 → 丢垃圾桶（糊了必须双击才清）」；底排「…熟了 4.5s 内点锅起锅 · 盘被占了就只能糊掉」；画面里 **2 口锅在窗口里**（起锅提示 + 倒计时环）· **1 口糊锅**（焦黑 + 红叉）· 5 个盘全部「热乎」+「∞ 可一直放着」（盘龄 0.4~90s）。**旧图在这里是「2 列在烧 + 5 盘保鲜」**。 |
| `测试截图/bf_columns.png`（更新） | 列对齐特写：三列摆成「**等在起锅窗口** / **刚起锅落盘（热乎）** / 还在烧」；底部操作说明按新口径改写。 |
| `测试截图/bf_plates_nocountdown.png`（更新） | 盘上永久保鲜对比图：9 盘改成「窗口内点锅起锅」落盘；代码口径那行改成「锅内：生 → 恰好 → **起锅窗口（4.5s，火候暂停）** → 糊」。 |
| `bf_pass.png` / `bf_fail.png` / `bf_result_exit.png` | 顺带重出（结算面板摹本「忘取」→「忘起锅」，机器人走新规则真打赢 / 真打输）。 |

出图**不是浏览器截图**：是把 `breakfast.js` 真发出的 Canvas2D 指令在 Node 里软件光栅化，
中文由 `tools/lib/text-compose.ps1` 用系统字体（微软雅黑）合成（本轮合成 347 段文字）。

---

## 7. 改动文件清单（只动了允许范围内的文件）

| 文件 | 改动 |
|---|---|
| `breakfast.js` | 规则 + UI（36 + 5 + 6 + 3 个逐字 job，全部经 `patch-literal.js`） |
| `tests/breakfast.test.cjs` | 14 + 5 + 1 个 job：改写 13 处旧口径断言 + 新增 4 例（bf-14①~④） |
| `tools/bf/headless.js` | 16 + 3 + 6 + 3 个 job：改写 9 段 + 新增 40 条 check |
| `tools/bf/e2e-audio.js` | 2 + 3 个 job：两条机器人 + 真实鼠标单击锅的新断言 |
| `tools/bf/shots/panel.js` | 10 + 3 个 job：`bot()` / 4 张图重摆 + 新增 `bf_pan_window.png` |
| **新增** `tools/bf/_patch/_bf14_pressure_probe.js` | 压力探针（63 条，验收第 3 条） |
| **新增** `tools/bf/_patch/_bf14_preflight.js` | 锚点预检器（落盘前把全部锚点验一遍，带逐行 diff） |
| **新增** `tools/bf/_patch/mk_bf14*.js` + `bf14*_*.txt` + `jobs_bf14*.json` | 全部逐字替换工件（审计用） |
| **新增** `tools/bf/_patch/bf14_snapshot_before.js` / `bf14_snapshot_after_r1.js` | 改前快照 / 第一轮改完快照（可精确回滚） |
| **新增** `早餐店-锅内限时起锅报告.md` | 本报告 |

**没有碰**：`mahjong.js`、`nlroute.js`、`index.html`、`tools/dev/stamp.js`、`art/fight2/**`
（其中 `index.html` 在本次会话期间**被其他人改动过**：326703 → 328852 字节，我一个字都没动；
`node tools/dev/check-inline.js` 对它当前版本的 2 段内联脚本检查仍是通过的）。

---

## 8. 未完成项 / 诚实说明

1. **「过火」在默认玩法里不再出现**（§1.1）。这是「窗口内暂停火候」的必然结果 —— 不暂停就不是
   `SERVE_WINDOW` 秒而是 `pw`（0.6~1.0s）秒。**曲线函数没改**，`manual` 那条路仍能完整观察四态；
   但如果你希望「过火」也回到默认玩法（例如窗口内仍继续烧，`burn` 宽限即窗口），请说一声 ——
   那是另一套数值（窗口 = `pw + burn`），我可以按同样方式改并有断言覆盖。
2. **`SERVE_WINDOW` 现值是 4.5s，不是需求里写的 3.5s**（3.5s 是「部分上餐回耐心」那个常量）。
   本轮**保持 4.5s**，理由与建议见 §3。要改的话只有一处常量。
3. **放开「盘占着也能下料」是有意的口径改动**（§1.2），它让旧原因码 `plate-occupied` 的触发点从
   「下料」搬到「起锅」。如果不放开，「盘被占 → 没地方放 → 糊」这条规则在真实操作里**永远触发不到**。
   代价：玩家可以在盘满时继续下锅，把一列「烧糊」—— 这正是用户要的那条压力。
4. **`BURNT_LIFE_MS` 成了死常量**（仅兼容保留，`step()` 不再读它）。「锅内糊残骸 14 秒自动清」
   这条旧行为**已删除**（与「必须双击才能再用锅」直接冲突）。无头/单测都没有断言依赖它。
5. **`autoPlate:true` 这条旧通道保留了**（调试 / 老回归用）：显式传它才会「一到恰好同帧落盘、不开窗口」。
   页面（`index.html`）**从不传**这个参数，所以玩家那一侧永远走新规则。单测里有一条专门验它。
   这意味着本仓库仍存在「页面到不了的第二条落盘路径」—— 如果你希望彻底删掉，我可以再删一轮。
6. **历史文档里的旧口径文案没改**：全文搜索后，游戏内文案（新手引导 / 底排说明 / 图例 / 顶栏 tip）
   都已是新口径，`index.html` **0 处**提及自动落盘；但 `docs/早餐店-列对齐改造报告.md`（第 42/60/63/76/92 行）
   与 `早餐店-盘上永久保鲜报告.md` 仍然描述旧规则 —— 它们是**历史轮次的报告**，
   且不在本轮允许改动的文件范围内（只许改 `breakfast.js` + `tests` + `tools/bf/**`），所以只报告、未改动。
7. **没有跑 `run-all-tests.ps1` 全量**（它包含麻将 / 格斗 / nlroute 等与本改动无关的套件）；
   只跑了与本改动相关且任务点名的 4 项 + 压力探针 + `tests/audio-paths.test.cjs`。
8. **`tools/e2e/bf.js`（不在验收名单、也不在允许改动范围内）没有跟着改**：它只生成中文报告，
   不驱动锅 / 盘（grep 过：0 处 `autoPlate` / `stations()[i].plate`），所以不受本轮影响 —— 但也没有真跑它。
9. **本轮的图仍不是浏览器截图**（沙箱起不了 Chrome 的 GUI 截图链路）；真 Chrome 那一条证据是
   `tools/bf/e2e-audio.js` 的 32 条断言（含真实 MouseEvent 单击锅）。
10. **`bf14_snapshot_*.js` 只是快照**，不是官方备份；`patch-literal.js` 的默认策略「每次改前刷新备份」
    使得 `breakfast.js.bf8bak` 现在 = 本轮**最后一轮改前**那一版（= 第十轮改完）。
    要精确回到 bf-14 之前，用 `tools/bf/_patch/bf14_snapshot_before.js`。

---

### 附：本轮全部逐字替换工件（审计用）

`tools/bf/_patch/jobs_bf14.json`（breakfast.js 36 job）· `jobs_bf14b.json`（5）· `jobs_bf14d.json`（6）·
`jobs_bf14f.json`（3）· `jobs_bf14_tests.json`（14）· `jobs_bf14c.json`（1）·
`jobs_bf14_headless.json`（16）· `jobs_bf14h2.json`（3）· `jobs_bf14e.json`（2）· `jobs_bf14g.json`（3）·
`jobs_bf14_panel.json`（10）· `jobs_bf14_panel2.json`（3），以及对应的 `bf14*_*.txt` 替换文本与
生成器 `mk_bf14*.js`。全部经 `tools/dev/patch-literal.js` 落盘（默认刷新备份 + `node --check` 语法闸），
每次落盘前都用 `_bf14_preflight.js` 把锚点验成「恰好命中 1 次」。
