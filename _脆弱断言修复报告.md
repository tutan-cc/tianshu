# 两处既有脆弱断言 · 修复报告

> 授权范围：只改 `tools/e2e/mj-browser.js` 与 `tools/test/mahjong-logic.js`（另在 `tools/dev/` 放了一个辅助闸）。
> 未碰 `mahjong.js` 业务逻辑、`breakfast.js`、`index.html`、`tools/dev/stamp.js`。**全程无 git 操作**，
> 所有改动走 `tools/dev/patch-literal.js`（默认每次刷新备份 + 唯一性 + 幂等 + 语法闸 + 失败回滚）。
> 依据：《小游戏自动化调测指南》§6「断言口径黄金法则 —— 只断言被操作动作自身的影响，不要读持续演化、
> 依赖真实时钟/真实负载的世界状态」。

---

## 0. 一句话结论

| # | 脆弱断言 | 原口径 | 新口径 | 结果 |
|---|---|---|---|---|
| 1 | `tools/e2e/mj-browser.js` 的「驱动到轮到人类」辅助 | 同步 for 循环 + 兜底 setHand（**没有终态分支**）→ 本局自己结束就必红 | 状态机：**轮询 → 终态判定 → 重开重试（≤3 局）** | e2e 连跑 3 次：**190/190 × 3 全过**，`ready_for_hint`/`hint_draw_fresh`/端到端不再红 |
| 2 | `tools/test/mahjong-logic.js` ⑦ 提示性能 | 60 次采样的**最坏值** < 300ms（极值统计量，无预热） | **p50（中位数）< 200ms 判死**，p95/最快/最坏只记录；预热 20 次 | 逻辑测试 5 连跑 **5/5 = 1181/1181**；4 并发 **4/4 = 1181/1181** |

两条断言一条都没放松，仍然是「必须真在人类回合拿到提示 / 点了金框那张牌 / 打完仍听」、
「提示计算本身不能变慢」。

---

## 1. 任务 1：e2e 的「驱动到轮到人类」辅助

### 1.1 为什么 `over/0/14` 必然红（失败机理，已确定性复现）

那三个随机红的断言（`ready_for_hint` / `hint_draw_fresh` / 端到端「摸牌→点金框」）都不是被测功能坏了，
而是它们**各自先要把牌局摆成「轮到自己」**，而旧辅助函数默认「牌局还活着」：

```
旧写法 = setHand(13 张) → for (i<60) { state(); ... act("draw") / act("pass"); break; } → 直接断言
```

它没有「本局已结束」这条分支，于是三种收场都是必红：

| 机制 | 触发 | 引擎里的那一步 | 断言读到的实测值 |
|---|---|---|---|
| ① 摸到的那张正好成胡 | 摆的 13 张是**听牌型**（123456789万 + 1234条 单吊 4条），墙首张是 4条 | `Engine.turn()` 内部 `evaluate()` 命中 → `settle()` 自摸 → `phase="over"`（`cur` 仍是 0、手牌已 +1） | **`over/0/14`** ← 历史日志里最常见的那次 |
| ② 牌墙摸完 | 局末墙空 | `Engine.turn()` 第一行 `if (wall.length===0) drawGame()` → `phase="over"` | `over/0/13` |
| ③ 进辅助函数前本局就结束了 | AI 自摸/流局 → `showResult()` → 8s 兜底 → `finishGame()` 置 `G.finished=true` | `dbgAct()` 第一行 `if (!E || G.finished) return false;`，怎么点都不动；`hintShouldCalc()` 也因 `!G.finished` 返回 null | `turn/0/13`，或整段提示直接跳过（**十来条 `hint_*` 一起变红 → 总数在 175/176 之间摆动**） |

**这不是推测，是复现**：探针 `tools/dev/_repro_drive.js` 把三种机制都做成确定性场景（旧写法片段 vs
从 `mj-browser.js` 的 HTA 字符串里**原样抽出来**的新辅助函数，一字未改）：

```
══ 机制①：摸到的那张正好成胡（这副牌单吊 4条）→ 自摸结算 ══
  牌墙首张 = 4条
  旧写法实测 = over/0/14 → ready_for_hint ✗ 红（这就是历史日志里的 over/0/14）
  新辅助实测 = turn/0/14 · ok=true games=1 restarts=0 drew=true
  → ready_for_hint 过 ✓

══ 机制②：牌墙摸完 → Engine.turn 直接 drawGame() 流局 ══
  旧写法实测 = over/0/13 → ready_for_hint ✗ 红
  新辅助实测 = turn/0/14 · ok=true games=2 restarts=1 drew=true（trace="over@0:1:over|"）
  → ready_for_hint 过 ✓

══ 机制③：本局在进辅助函数之前就已彻底结束（G.finished=true）══
  旧写法实测 = turn/0/13 · finished=true → ready_for_hint ✗ 红
  新辅助实测 = turn/0/14 · finished=false · ok=true games=2 restarts=1 drew=true
  → ready_for_hint 过 ✓
```

### 1.2 新逻辑的状态机

```
mjDriveToHumanTurn(hand, wantDraw, maxGames = 3)
  对每一局 g = 0..2:
      g > 0 → mjRestartGame()            // dispose() + start()，重开一局并恢复可玩态
      setHand(hand, [], null)            // 沿用既有调试摆牌（守恒版，只搬牌不造牌）
      轮询最多 80 次：
          st = state()
          · 终态①「未结束 且 phase=turn 且 cur=0 且 手牌%3==2」→ return { ok:true, drew, games, polls, restarts, trace }
          · 终态②「finished 或 phase===over」          → 记 trace，跳出内层 → 外层重开一局重试
          · 自己的 claim / rob 窗口                     → act("pass")，回到轮询头部
          · 轮到自己但只有 13 张                        → wantDraw ? 真 act("draw") : break
          · 别家的回合                                  → Mahjong.debug.step() 走一拍（与 pump() 同一条路径），回到轮询头部
  三局都不成 → return { ok:false, st, trace }   // 断言照旧红，并把三局的状态轨迹带进 info
```

关键点：

* **终态判定是双条件的** —— 「轮到人类」与「本局已结束」都算终态，两者都不满足才继续推进（而不是像旧代码
  那样默认还活着）；
* **重开是有界的**：最多 3 局。重开后牌墙是**新洗的**（`new Engine()` → `shuffle(createWall())`），所以
  机制①那种「又摸到成胡张」在下一局是独立事件，残余失败概率 ≈ (3/83)³ ≈ 5e-5；
* **断言表达式一个字没改**（`!!(dr.ok && st && st.phase==="turn" && st.cur===0 && st.handCount%3===2)`），
  辅助函数只负责把前置状态**确定性地**摆好；
* **不改断言条数**：5 个调用点照旧用原来的断言名（`ready_for_chain` / `ready_for_hint` / `voice_turn_ready` /
  `hint_setHand` / `hint_draw_fresh`），驱动过程只写进 `info.driveChain / driveVoice / driveHint / driveHintSet /
  driveTenpai`（`{ok, drew, games, polls, restarts, trace}`），失败时可以从 3 次 e2e 的 `[Trident 探针原始]` 里直接读到。

改造的 5 个调用点（都是同一段「同步 for + 兜底」的复制粘贴）：

| 位置 | 断言 | 摆法 |
|---|---|---|
| `step2b` | `ready_for_chain` | 14 张直接可打 |
| `step2b2` | `ready_for_hint` | 13 张听牌型 + 真摸一张 |
| `stepVoice` | `voice_turn_ready` | 14 张直接可打 |
| `stepHint` | `hint_setHand` | 14 张（提示用） |
| `stepHintTenpai` ① | `hint_draw_fresh` | 13 张 + 真摸一张（`drew` 由驱动的 `drew===true` 决定，**仍然要求真的摸过牌**） |

> 顺带修掉的一个隐患：`stepHint` 原来只调一次 `setHand`，本局若已 `finished`，`hintNow()` 返回 null →
> 走「跳过」分支 → 后面十几条 `hint_*` 断言整体变成失败 —— 这正是历史日志里**总数在 175/176 之间摆动**的来源。

### 1.3 e2e 实跑结果（3 次）

| 次 | 模式 | 通过 / 失败 | 驱动诊断 | jsErrors |
|---|---|---|---|---|
| 1 | trident-hta | **190 / 0** | chain ok(games=1,polls=1) · voice ok(1,1) · hint ok(1,2,drew=true) · hintSet ok(1,1) · tenpai ok(1,2,drew=true) | "" |
| 2 | trident-hta | **190 / 0** | 同上（restarts 全 0 · drew 全 true） | "" |
| 3 | trident-hta | **190 / 0** | 同上 | "" |

`ready_for_hint` / `hint_draw_fresh` / `hint_tenpai_*` 在 3 次里都是实测值 `turn/0/14`、`drew=<牌> hint=<牌>
hint.drawn=<同一张> mark=<同一张>`、`clicked=5筒 hint=5筒`、`3条 hand=13`，没有任何 `over/*` 出现。

---

## 2. 任务 2：⑦ 提示性能断言

### 2.1 原口径为什么是「负载型」

```
原：for (it=0; it<60; it++) { 洗一手随机牌; hintCacheClear(); t0=Date.now(); hintCalc(); ms=...; worst=max(worst,ms) }
    ok(worst < 300, "⑦ 单次提示计算最坏耗时 ... < 300ms（平均 ...）")
```

`worst` = 60 个样本的**极值**。极值统计量的方差极大：60 次里只要有一次被打断（GC、杀软扫描、同机别的
node/浏览器、CPU 降频、系统录制软件），那一次就会顶上去。实测证据：

| 来源 | 最坏值 | 原口径（<300ms）判定 |
|---|---|---|
| 改前基线（原始代码，本次第一跑） | **350ms**（平均 90ms） | **红** |
| 历史日志（任务里给的） | 311 / 334 / 358 / 369 / 546 / 756ms（并发时为这些；空载 145~265ms） | 并发时红 |
| 本次改后采样里的「同一个量」（60 次、无预热口径下的最坏） | 482 / 183 / 165 / 152 / 116ms | 5 次里红 1 次 |
| 同上（4 并发） | 222 / 227 / 227 / 330ms | 4 次里红 1 次 |

也就是说 **300ms 这条线本来就压在噪声里**：同一份算法、同一台机器，最坏值能横跨 116~604ms。
它测的不是提示算法，是「谁抢了 CPU」。

### 2.2 新口径

```
新：预热 20 次（吃掉 JIT 冷启动 —— 旧口径那个「平均 90ms」有很大一部分就是冷启动）
    → 采样 40 次 → msAsc 升序
    ok(p50 < 200ms)                       // 唯一判死项（= 旧口径里那一条断言，条数不变）
    console.log(最快 / p50 / p95 / 最坏 / 平均)   // 只记录、不判死
    MJ_HINT_STRICT=1      → 额外 ok(p50 < 100ms)   // 安静机器/CI 独占机上的严格口径（默认关闭）
    MJ_HINT_ASSERT_WORST=1 → 额外 ok(worst < 600ms) // 想把最坏值当硬断言跑（默认关闭）
```

* **为什么 p50 稳**：要有**半数**样本被拖慢才会动，单次抖动改不了它；
* **为什么仍然是「同一件事」**：判死的还是「单次提示计算耗时」，只是从极值换成中位数；
* **预热 20 + 采样 40 = 60 次，与旧循环逐次同构**（每轮一次 135 次交换的洗牌）→ 消耗的 `Math.random`
  序列与改前一模一样，**不会因为本轮多洗了几次牌把 §22 那些随机用例的结果带偏**（这是改测试最容易
  忽略的副作用，我刻意保住了它）。

### 2.3 实测数据表（同一台机器 · 同一份算法 · 只换口径）

**改前口径下的判据（worst）**：见 2.1 —— 5 次里 1 次红、并发 4 次里 1 次红，改前基线一跑就红。

**改后（20 预热 + 40 采样）**：

| 场景 | 最快 | **p50（判死）** | p95 | 最坏（仅记录） | 平均 | 结果 |
|---|---|---|---|---|---|---|
| 空载 第 1 跑 | 21 | **65** | 191 | 455 | 81 | 1181/1181 ✔ |
| 空载 第 2 跑 | 16 | **34** | 113 | 202 | 45 | 1181/1181 ✔ |
| 空载 第 3 跑 | 20 | **44** | 88 | 92 | 48 | 1181/1181 ✔ |
| 空载 第 4 跑 | 13 | **44** | 167 | 203 | 57 | 1181/1181 ✔ |
| 空载 第 5 跑 | 13 | **43** | 100 | 115 | 50 | 1181/1181 ✔ |
| 4 并发 #1 | 24 | **83** | 229 | 237 | 92 | 1181/1181 ✔ |
| 4 并发 #2 | 21 | **78** | 169 | 198 | 80 | 1181/1181 ✔ |
| 4 并发 #3 | 22 | **70** | 155 | 177 | 81 | 1181/1181 ✔ |
| 4 并发 #4 | 28 | **86** | 190 | 257 | 96 | 1181/1181 ✔ |
| 机器另有重负载时（观测） | 9~59 | **95 / 107 / 113 / 121 / 126 / 131 / 134 / 135** | 76~886 | 85~957 | — | 仍全部通过 |

**阈值 200ms 的来处**：实测 p50 的最坏值是 **135ms**（4 并发 86ms、带额外负载 135ms），200ms 是它的
**1.5 倍**；而旧口径的最坏值动辄 300ms+，等于没有余量。噪音带本身（安静 34~65ms ↔ 有负载 70~135ms，
极端 264ms）已在注释里写清楚。

### 2.4 「能真检出算法慢 3 倍」的实测证明

把提示计算的工作量**真的做成 3 倍**（每个样本连算 3 手不同的牌，避开引擎内部 memo），再跑：

```
★3 倍回归模拟 · 默认口径：
  提示耗时分布：最快 118ms · p50 229ms · p95 338ms · 最坏 373ms
  通过 1180 / 共 1181，失败 1
   - ⑦ 提示计算中位数 p50 229ms < 200ms   ← 红 ✔
★3 倍回归模拟 · MJ_HINT_STRICT=1：p50 223ms ≥ 200ms 与 ≥100ms 两条都红 ✔
（验证完立刻用反向 patch 还原，还原后 node --check ✓、再跑一次 1181/1181 ✓）
```

诚实交代（也写在代码注释里）：这台机器的墙钟噪声带本身就有 ~3 倍（安静 p50≈40ms ↔ 有负载 p50≈120ms，
而「算法慢 3 倍」正好也是这个量级）。所以**在完全安静的机器上**、且基线恰好落在最低档（p50≈34ms）时，
3 倍回归（≈102ms）不会越过默认的 200ms —— 那种情况下用 `MJ_HINT_STRICT=1`（p50<100ms）抓。
我试过用「同机参照工作量」做归一化（比值 = 提示耗时 ÷ 参照耗时）来同时满足两者：参照活自己就有
±60% 抖动，比值反而比裸 p50 更飘（实测比值 0.21~0.94 横跨 4.5 倍），所以**放弃**，没有把它留在代码里
（避免留一段看起来聪明、实际更脆的机制）。

---

## 3. 验收实跑记录

| # | 项目 | 命令 | 结果 |
|---|---|---|---|
| 1 | 语法闸 | `node --check tools/e2e/mj-browser.js` / `node --check tools/test/mahjong-logic.js` | 两个都 OK |
| 1b | 内联胶水 | `node tools/dev/check-inline.js` | 内联脚本 2 段，失败 0 |
| 1c | **额外加的闸** | `node tools/dev/_hta_check.js` | 把 `mj-browser.js` 里的 HTA 字符串拼成真脚本 → 内联 JScript 52,699 字符 **vm.Script 编译通过**（`node --check` 查不到拼出来的浏览器脚本，改内联层必须补这一闸） |
| 2 | 逻辑层 5 连跑 | `node tools/test/mahjong-logic.js` ×5 | **5/5 = 1181/1181 全部通过**（尾部见 2.3 表） |
| 3 | 负载下 4 并发 | 4 × `node tools/test/mahjong-logic.js` | **4/4 = 1181/1181 全部通过**（p50 70/78/83/86ms） |
| 4 | e2e 3 连跑 | `node tools/e2e/mj-browser.js` ×3 | **3/3 = 通过 190 / 失败 0**，失败名里不再出现 `ready_for_hint`/`hint_draw_fresh`/`hint_tenpai_*` |
| 5 | 回归 | `node tests/breakfast.test.cjs` | **tests 83 / pass 83 / fail 0** |
| 5b | 回归 | `node tools/bf/headless.js` | **通过 465，失败 0** |

产物：`tools/dev/_final_5runs.log`（5 连跑）、`tools/dev/_final_load4_run{1..4}.log`（4 并发）、
`tools/dev/_hta_check.js`（HTA 语法闸）、`tools/dev/_repro_drive.js`（失败机理复现探针）。

---

## 4. 未完成项 / 诚实交代

1. **§22 的随机用例仍有既有偶发红，本次未改（不在授权范围内）**。实测抓到两条：
   * 一次 5 连跑里：`1 家碰下这张 7筒 → 期望 true，实际 false` + `碰事件播的是 碰.mp3 → 杠.mp3,7筒.mp3`；
   * 一次 4 并发里：`22.2 三局共审计 175 拍（每拍一次，不是只审首尾）`。
   这些都是「依赖随机牌局结果/拍数」的断言，与本次两处无关：为了让本轮改动**不把随机流带偏**，⑦ 的
   采样次数刻意保持 **60 次**（20 预热 + 40 采样），与旧循环逐次同构 —— 也就是说改后 §22 看到的随机
   序列与改前完全一致，出现红就是它自己本来就有的。建议后续单独授权再处理。
2. **性能断言的极限**：默认 200ms 口径在「安静机器 + 恰好最低基线」时抓不住 3 倍回归（见 2.4 的诚实交代），
   需要 `MJ_HINT_STRICT=1` 补刀。这是墙钟测量的物理下限，不是阈值调参问题（归一化方案已实测否决）。
3. **浏览器侧的同类断言 `hint_fast`（`<300ms`，最近/最坏）没动** —— 它也在读墙钟，且判的是 `worstMs`，
   严格说同样偏脆（本次 3 次 e2e 实测 `8/42`、`9/53`、`9/54`，余量还够）。任务只授权改那两处，故未动，
   列在这里备查。
4. **Chrome 模式（模式 A）本来就过不去**（每次 5~7 项固定未过，然后降级 Trident），3 次 e2e 的未过项
   内容一致，属于既有的沙箱环境限制，与本次改动无关。
5. **实测环境说明**：本次测量期间机器上有 EVCapture 录制 + Feishu/Doubao/浏览器等常驻负载（CPU 负载一度
   79%），这既是噪音来源，也正好把「负载下是否稳」验得比较狠（连 264ms 的 p50 都出现过一次，那次的成因
   是我自己残留的测试进程，已定位并清理）。
