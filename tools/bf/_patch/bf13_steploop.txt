    /* 专属盘：**永久保鲜**（bf-13）。这里只刷新「放了多久」这个记录值 p.age ——
       不降档、不倒数、不判过期、也不自动清盘：盘上那份放多久都在、放多久都是最高档。
       （锅内那条「忘出锅 → 糊」的路径在上面那一段，两条路径互不影响。） */
    for (i = 0; i < st.plates.length; i++) refreshPlate(st, st.plates[i]);
