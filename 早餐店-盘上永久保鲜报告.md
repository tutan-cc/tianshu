# 早餐店 · 备用餐盘「永久保鲜」改造报告（bf-13）

> 需求原话：**「帮我修改一下早餐游戏的备用餐盘，就是做好的早餐放旁边的盘子不要有倒计时，可以一直放着不会冷掉」**

一句话结论：**盘上的食物从「会降档 / 有倒计时 / 超时变糊 / 糊了 14 秒自动清」改成永久保鲜 ——
落盘那一刻是什么档就永远是什么档（完美份恒为「热乎」14 分），没有倒计时、不会凉、不会糊、不会自己消失。
锅内「生 → 恰好 → 过火 → 糊」一个字没改。**

---

## 1. 现状表（**改前**）

| # | 维度 | 改前行为 | 位置（改前行号） |
|---|---|---|---|
| 1 | 常量 | `SERVE_WINDOW = 4.5`（「过火计时」）· `HEAT = { hotSec:1.5, warmSec:3.0, coldSec:4.5, hot:14, warm:10, cold:6, overPenalty:-3 }` · `BURNT_LIFE_MS = 14000` | 144–158 |
| 2 | 盘上过期判定 | `plateBurntAt(age) = age >= 4.5`（边界：4.49 不糊 / 4.51 糊）· `plateLeftSec(age) = max(0, 4.5-age)` · `plateExpired(st,p)` 读盘龄 | 251 / 253 / 825 |
| 3 | 盘上降档 | `refreshPlate()` 用 `heatTierOf(p.state, age)` → 每 1.5s 掉一档：热乎 → 温 → 凉 | 813–823 |
| 4 | 盘上变糊 | `step()` 的盘循环：`plateExpired` → `state="burnt"`、`st.burnt++`、`st.expire++`、冒烟、飘字「忘取了 · 糊了！」、事件 `plate-burnt` | 1300–1309 |
| 5 | 盘上消失 | 糊盘残骸按壁钟 14s 后 `st.plates.splice(i,1)`（整盘自动清） | 1310–1314 |
| 6 | 盘状态机 | `platePhaseOf(p, elapsed)`：`plated → hot → warm → cold → burnt` | 852–859 |
| 7 | 落盘档位 | `mkPlate()` 记 `left = plateLeftSec(0) = 4.5`、`burning`、`burntAt` | 805–811 |
| 8 | 单盘提示 | 单击盘没人要 → 提示「现在没人要这份 —— 先放着（**会变凉**）」 | 1065 |
| 9 | UI · 顶栏 tip | 「③ 双击盘 → 丢垃圾桶（不扣分）· **盘上停留超过 4.5s 会糊，糊了只能双击丢掉**」 | 2186 |
| 10 | UI · 盘上小字 | 第一行「煎蛋·热乎」，第二行「**4.1s 内送出**」（≤1s 红闪） | 2878–2888 |
| 11 | UI · 底排说明 | 「9 列 × 每列 1 锅 1 专属盘 · **盘上停留超过 4.5s 会糊**，糊了只能双击丢掉」 | 2895 |
| 12 | UI · 操作图例 | 三条：点食材 / 点专属盘 / 双击盘（**没有保鲜说明**） | 2907 |
| 13 | UI · 顶栏明细 | 「… 盘上 5/9 · **过火计时 4.5s**」 | 2526–2528 |
| 14 | 结算 / debug 字段 | `plateLife = SERVE_WINDOW(4.5)`、`serveWindow = 4.5` | 1392 / 3270 / 3431 |
| 15 | 出图脚本 | `bf_game.png` 摆「热乎 / 温 / 凉 / 糊了」四态；`bf_columns.png` 注解写「盘上停留超过 4.5s 会糊」 | panel.js ①/② |
| 16 | 断言 | tests 83 例里 **5 例**断言盘上会降档 / 会过期 / 会糊 / 有倒计时；headless 465 条里 **9 处**同类 | 见 §5 |
| 17 | 锅内路径 | 生 → 恰好 → 过火 → 糊（`cookState`），`autoPlate` 默认**开**：一到「恰好」同帧落盘 | 222–230 / 2168 / 745 |

---

## 2. 改动点（**改后**）

只改了 `breakfast.js`（+ 测试/无头/出图脚本）。**没有碰** `mahjong.js` / `nlroute.js` / `index.html` / `tools/dev/stamp.js` / `art/fight2/**`。

| # | 维度 | 改后行为 | 位置（改后行号） |
|---|---|---|---|
| 1 | 新常量 | `PLATE_KEEP = true`（唯一口径）· `PLATE_NO_TIMER = Infinity`（「没有倒计时」哨兵）· `PLATE_KEEP_TEXT = "∞ 可一直放着"` | 171–172 / 1875 |
| 2 | 过期判定 | `plateBurntAt(age)` **恒 false**；`plateLeftSec(age)` **恒 Infinity**；`plateExpired(st,p)` **恒 false**（名字/签名保留，语义 = 「盘上没有这条路径」） | 274 / 277 / 857 |
| 3 | 盘上不降档 | `refreshPlate()` 只更新 `p.age`（纯记录），档位一律 `heatTierOf(p.state, 0)` = **冻结在落盘那一刻**；`p.left = PLATE_NO_TIMER`；`p.burning = false` | 845–855 |
| 4 | 盘上不会糊 | `step()` 里那段「过期 → 变糊 + 记账 + 冒烟 + 飘字」整段删除，只剩 `refreshPlate` | 1331–1334 |
| 5 | 盘上不消失 | 糊盘残骸「14 秒自动清」整段删除 → 盘上那份只有玩家双击才走 | 1331–1334 |
| 6 | 盘状态机 | `platePhaseOf(p, elapsed)` → 恒为落盘档（`burnt` 只在 `p.state` 本身就是 burnt 时返回，即「锅里糊了再端上盘」） | 888–893 |
| 7 | 落盘档位 | `mkPlate()`：`tier = heatTierOf(state, 0)`、`left = PLATE_NO_TIMER` | 834–840 |
| 8 | 单盘提示 | 「现在没人要这份 —— 先放着（**不会凉，放多久都能上**）」 | 1101 |
| 9 | UI · 顶栏 tip | 「③ 双击盘 → 丢垃圾桶（不扣分）· **盘上永久保鲜：没有倒计时、不会凉、不会糊，放多久都能上**」 | 2211 |
| 10 | UI · 盘上小字 | 第一行「食材·热乎」（不红闪），第二行**静态**「**∞ 可一直放着**」；`N.Ns 内送出` / 红闪 / 倒数计时全部删除 | 2905–2915 |
| 11 | UI · 底排说明 | 「… **盘上永久保鲜：没有倒计时 / 不会凉 / 不会糊，放多久都能上**」 | 2923 |
| 12 | UI · 操作图例 | 加第 ④ 条「**盘上永久保鲜：不会凉 · 不会糊**」（原三条一字不改） | 2935 |
| 13 | UI · 顶栏明细 | 「… 盘上 5/9 · **盘上永久保鲜（无倒计时）**」 | 2553 |
| 14 | 结算 / debug 字段 | `plateLife = Infinity`、新增 `plateKeep = true`；`serveWindow`（锅内窗口）仍 4.5（兼容层） | 1413–1414 / 3301（debug.state）/ 3431（debug.prepMap） |
| 15 | 出图脚本 | `bf_game.png` 改成 5 盘盘龄 0.4/2.2/3.6/30/90s **全为热乎**；`bf_columns.png` 注解改成「盘上永久保鲜…」；**新增** `bf_plates_nocountdown.png` | panel.js ①/②/②b |
| 16 | 断言 | 5 例 + 9 处按新口径改写（**一条都没删**），新增 3 例 + 21 条；tests **86/86**、headless **486/486** | §5 / §6 |
| 17 | 锅内路径 | **一个字没改**（`cookState` / `autoPlateStations` / `takeReady` / 窗口兼容层原样） | 243–251 / 967–982 |

**风险点复核**：`plateLeftSec() ≡ Infinity` 会让 `JSON.stringify(p.left)` 变成 `null`（Infinity 不是合法 JSON 数）。
已核对：**页面层（index.html）从不读 `plateLife` / `left`**（源码里 0 处），读它的只有 headless（已同步改成 `!isFinite(...)`）。
`heatTierOf()` 这条「越放越凉」的曲线**保留**（纯函数单测仍在验它，锅内 `over → 温档` 的换算仍用它），
只是**盘上路径一律传 age = 0**。

---

## 3. 这条改动把哪些压力去掉了（附**实测**证据）

新增探针 `tools/bf/_patch/_bf13_pressure_probe.js`（`node tools/bf/_patch/_bf13_pressure_probe.js`），
三组对照实测（「点完 9 样食材，然后**什么都不做** 60 秒游戏时间」）：

### A) 页面默认（`autoPlate` 打开）—— 实测
```
锅上出现过的状态帧：{congee:raw:204, milk:raw:119, … juice:raw:72}   ← 只有 raw，没有任何 perfect/over/burnt
糊掉份数 st.burnt = 0 · 忘取 st.expire = 0 · 丢弃 st.tossed = 0
盘上份数 = 9 / 9   ← 盘龄 56.6 ~ 58.8s，全部 state=perfect tier=hot left=Infinity burning=false
放 58.8s 后上餐：{"ok":true,"kind":"perfect-hot","heat":"hot","delta":14}
```

### B) 对照 `autoPlate:false`（＝旧「按住看火」那条路，**页面从不传这个开关**）—— 实测
```
锅上出现过的状态帧：{… juice:perfect:35, juice:over:43, juice:burnt:840, … congee:burnt:840}
糊掉份数 st.burnt = 9 · 盘上份数 = 0 / 9
```

### C) 口径自检 —— 实测
```
PLATE_KEEP = true · PLATE_NO_TIMER = Infinity
plateBurntAt(0 / 4.49 / 4.5 / 60 / 1e6) = false / false / false / false / false
plateLeftSec(0 / 4.5 / 600)             = Infinity / Infinity / Infinity
plateExpired(空状态)                     = false
```

### 结论（**要请用户拍板的地方**）
1. **`autoPlate` 的实际默认值 = 打开（true），且页面永远走默认**：
   * `newState(cfg)`：`autoPlate: cfg.autoPlate !== false`（breakfast.js:773）
   * `start(hostEl, opts)`：`autoPlate: opts.autoPlate !== false`（breakfast.js:2193）
   * **`index.html` 里 0 处 `autoPlate`** → 玩家那一侧永远是「熟了自动落盘」。
2. 因此 **A 组实测已经说明**：即使在改动**之前**，「锅里忘取就糊」在默认玩法里也**根本触发不到** ——
   食物一到「恰好」就在**同一帧**离开锅（`autoPlateStations`），锅上只会出现 `raw`。
   「忘取就报废」这条压力原本**完全由盘上的 4.5s 过火计时承担**。
3. **本次把盘上那条链去掉之后，默认玩法下「做好的早餐会报废」这件事整体消失了**：
   * 去掉的：盘上降档（14 → 10 → 6 分）、盘上 4.5s 倒计时、盘上变糊、糊盘 14s 自动清、`st.expire`（忘取）计数。
   * **留下来的**：顾客耐心 / 跑单（−8，气走 5 位提前失败）、上错菜（−5）、**盘占用（`plate-occupied`：
     盘里有一份时这一列不能再下料）**、**双击丢弃仍是玩家操作**、锅内火候（默认玩法下几乎不出现，见上）、
     75 秒 / 8 位的时间与产能压力、以及「送得越早越好」这件事现在只由**顾客耐心**衡量。
   * 也就是说：**难度整体下降**，剩下的是「产能 + 排单 + 耐心」三条，不再有「手滑放凉了」的惩罚。

---

## 4. 两个可选的折中（**按要求：只报告，没有实现**）

### 折中 ① 保留锅内「恰好 → 过火 → 糊」作为唯一压力点
* **做法**：把 `autoPlate` 默认关掉（或改成「完美窗口内不自动落盘，要玩家点一下锅/盘才出锅」），
  让玩家真的面对「这锅再不管就糊了」。
* **代价**：改动面比现在大 —— `autoPlate` 是 bf-3 起的核心规则，关掉等于换一套操作节奏
  （点食材 → **盯火候 → 手动出锅** → 点盘送客），现有 9 条下锅音效 / 自动落盘的 `熟了 · 落到专属盘` 反馈、
  以及 headless 里一大票「熟了自动落盘」的断言都要跟着改。
* **效果预估**：压力**最接近改前**，但玩家可能觉得「怎么又要我盯 9 口锅」。
  另外锅内宽限只有 0.7~1.2s（`burn`），比原来的盘上 4.5s **更严苛**。

### 折中 ② 盘上不降档，但「丢弃 / 覆盖」仍需操作（**这是当前代码的现状**）
* **做法**：维持现在的永久保鲜；把「盘里有一份 → 这一列不能再下料」（`plate-occupied`）
  和新加的「双击才丢得掉」当作盘上唯一的操作成本。
* **代价**：几乎没有新增改动（现状即此），但**压力很轻** —— 玩家只要记得双击/送掉就不会有任何损失，
  「放久了」这件事彻底没有代价。
* **可选加码**（都还没做，等用户点头）：① 同列盘里已有别的东西时允许**直接覆盖**（省一次双击，但也更容易误操作）；
  ② 给「盘上积压」加软提示（例如 9 盘全满时给一次「盘都满了，快送出去」的提示）而不加惩罚。

> 两个折中**都没有动手**。当前实现 = 用户原话的字面口径（永久保鲜、零惩罚）。

---

## 5. 断言改动逐条对照（原期望 → 新期望 → 原因）

### 5.1 `tests/breakfast.test.cjs`：5 例改写（**一条断言都没删**，只改期望 + 加断言）

| 用例 | 原期望 | 新期望 | 原因 |
|---|---|---|---|
| 「单击盘：此刻没人要这份…」（现名：…永久保鲜） | 放 2.0s → `tier="warm"`；再 1.5s → `tier="cold"`；上餐 `heat="cold"` `delta=6` | 放 2.0s → `tier="hot"`；再 1.5s → `tier="hot"`、`state="perfect"`、`left=Infinity`；上餐 `heat="hot"` `delta=14` | 盘上不再降档 → 档位与盘龄无关，分数恒为最高档 |
| 「9 盘 + 过火报废…」（现名：9 盘 + 永久保鲜） | `plateBurntAt(4.51)=true`、`plateBurntAt(4.5)=true`；`plateLeftSec(0)=4.5`、`plateLeftSec(5.5)=0`；真实推进「窗口后 0.01s → `state="burnt"`、`burnt=1`、`expire=1`、单击被拒」；自然推进 4.6s → `burnt` | 同三个纯函数**一致地恒为** `false` / `Infinity`（并补 `plateBurntAt(60)/plateBurntAt(1e6)`、`plateLeftSec(600)`）；真实推进「窗口后 0.01s → `perfect`、`tier="hot"`、`burning=false`、`burnt=0`、`expire=0`、上餐 `delta=14`」；自然推进 4.6s → `perfect`/`hot`/`burnt=0`/上餐 14 | 「过火计时」这条路径整体不存在了（用户要的正是取消它） |
| 「热乎度三档…」（现名：热乎度曲线仍是纯函数的三档…盘上不再走这条曲线） | `heatTierOf` / `serveScore` 的 10 条纯函数断言；三档上餐 `[[0.5,"hot",14],[2.2,"warm",10],[4.0,"cold",6]]`；「档位只降不升」`seq=["hot","warm","cold"]` | 纯函数 10 条**原样保留**；上餐改 `[0.5, 2.2, 4.0, **30**]` → 全部 `hot`/`14`；档位序列改 `seq=["hot"]`（8 秒一次都没变） | 曲线定义没改（单测继续验它），但**盘上不再走这条曲线** |
| 「灶位 / 盘状态机是纯函数…」（现名：…盘恒为落盘档） | `platePhaseOf(+hotSec+0.01)="warm"`、`(+warmSec+0.01)="cold"`、`(+SERVE_WINDOW+0.01)="burnt"` | 三条全为 `"hot"`，并补 `+3600`、`+1e9` → `"hot"` | 同上；`elapsed` 参数不再参与盘上档位 |
| 「过火计时从「落盘那一刻」起算…」（现名：盘上永久保鲜…一直保持） | `p.left > 4.5-0.3`；4.6s 后 `state="burnt"` | `p.left === Infinity`；4.6s 后 `perfect`/`hot`/`burnt=0`/`expire=0`；再补「放到 60s 仍 `perfect`/`hot`」 | 盘上不再起算任何计时 |

### 5.2 `tests/breakfast.test.cjs`：新增 3 例（对应验收要求）
| 新用例 | 断言要点 |
|---|---|
| **永久保鲜①**：盘上放满 60 秒（游戏时间）仍是「热乎」最高档，仍能吃 14 分 | 60s 后 `state=perfect` / `tier=hot` / `hot=true` / `left=Infinity` / `age≥60` / `burnt=0` / `expire=0`；上餐 `kind="perfect-hot"` `delta=14`、分数只涨 14、盘清空、灶位恢复 |
| **永久保鲜②**：盘上食物不会进入 burnt 分支 —— 不糊 / 不冒烟 / 不消失 / 不堵死 | 120s 后仍在盘上、`state=perfect`、`tier=hot`、`burning=false`、`platePhaseOf≠burnt`、`plateExpired=false`、`plateBurntAt=false`、`plates.length=1`、`burnt=0`、`expire=0`、`tossed=0`、`smoke=0`、无「糊」飘字；下料原因码仍是 `plate-occupied`（**不是** `burnt`）；双击照样丢得掉 |
| **永久保鲜③**：锅内路径不受影响 —— 锅里该糊还是糊 | ③a 按住看火：2.2s 恰好 → 过火 → 3.8s **锅内糊**、`st.burnt=1`、`phaseOf="burnt"`、`station-occupied`、单击盘 `no-plate`、双击才清；③b 默认自动落盘 60s：`burnt=0` 且盘上 60s 后仍 `hot`；③c 锅内糊 → `takePlate` 端上盘 → `state=burnt`、单击被拒 `why="burnt"`、放 30s 仍是糊盘且**不会自动消失**、只能双击丢 |

### 5.3 `tools/bf/headless.js`：9 处改写（`A()` 断言 399 → 420，**净 +21**，无删除）

| 段 | 原期望 | 新期望 | 原因 |
|---|---|---|---|
| ⑥ 盘交互 | 「盘上停留超过 4.5s → 变糊」；随后「单击被拒/双击清空」基于**过期糊盘** | 「超过 4.5s **也不糊**」+ 新增「放 90s 仍 `hot`」「`left=Infinity`」「`expire=0`」「锅内照样会糊」「`burnPlate` 造糊盘成功」；**单击被拒 / 双击清空 / 不扣分 全部原样保留**（糊盘改由「锅内糊 → 端上盘」这条唯一真实路径制造） | 盘上不再过期；但「糊盘」这条交互仍要验，来源换成锅内 |
| ⑥.5 常量 | `plateLife===4.5 && serveWindow===4.5` | `!isFinite(plateLife) && plateKeep===true && serveWindow===4.5` | 盘上寿命 = ∞；锅内窗口常量保留 |
| ⑥.5 落盘 | `p0.left > 4.0 && p0.left <= 4.5` | `!isFinite(p0.left)` | 没有倒计时 |
| ⑥.5 降档 | 停 1.7s → `tier="warm"`；停 3.2s → `tier="cold"` | 停 1.7s / 3.2s / **90s** 全部 `tier="hot"`（新增 90s 一条） | 档位不再随盘龄变 |
| ⑥.5 上餐 | `heat==="cold" && delta===6` | `heat==="hot" && delta===14` | 最高档 |
| ⑥.5 没人要 | 窗口后 0.05s → `burnt`、`expire===1`、`serveCol().why==="burnt"` | → `perfect`、`tier="hot"`、`expire===0`、**放 60s 仍在**、`serveCol().why==="no-want"`（新增 3 条）；双击丢弃断言保留 | 盘上不会报废，「没人要」与「糊了」彻底分开 |
| ⑥.6 边界 | 窗口前 `perfect`/`cold`、窗口后 `burnt`、`burnt=1`、`expire=1` | 窗口前后**都** `perfect`/`hot`、`burnt=0`、`expire=0`、放 90s 仍 `perfect`/`hot`、`plateCount=1`（新增 5 条）；糊盘改由「锅内糊 → `burnPlate` → 上桌 −5 / 跑单 +1」这条链验（**原断言保留**） | 同上 + 锅内路径回归 |
| ⑥.7 渲染 | 「盘上有食物时画出档位 + `N.Ns 内送出`」；「4.5s 后画出`糊了 · 只能丢`」 | 「画出 `煎蛋·热乎`」+「画出静态 `∞ 可一直放着`」+「**不存在**任何 `内送出` 文案」+「放 90s 文案与档位不变、不画糊」（新增 5 条）；糊盘红叉渲染改由真实路径制造后**断言保留** | UI 文案必须与行为一致（盘上不许再有倒计时） |
| ⑨d 贴图 | `d5.burnPlate(6)`（拨表造糊） | 先 `d5.trash(6)` 再 `burnPlate(6)`，并新增「造糊盘成功」一条 | 盘上已有包子盘 → 新的 `burnPlate` 走「锅内糊 → 落盘」真实路径 |

---

## 6. 全部测试数字（**本次真跑**）

| 验收项 | 命令 | 基线 | 改后 | 结果 |
|---|---|---|---|---|
| 语法 | `node --check breakfast.js` | exit 0 | **exit 0** | ✔ |
| 内联语法 | `node tools/dev/check-inline.js` | 2 段 / 失败 0 | 2 段 / **失败 0** | ✔ |
| 纯逻辑单测 | `node tests/breakfast.test.cjs` | 83 / 83 | **86 / 86**（83 + 新增 3 例） | ✔ 0 失败 |
| 无头渲染验收 | `node tools/bf/headless.js` | 465 / 465 | **486 / 486** | ✔ 0 失败 |
| 真 Chrome 音效 | `node tools/bf/e2e-audio.js` | 31 / 31 | **31 / 31** | ✔ 0 失败 |
| 音频清单（顺带回归） | `node tests/audio-paths.test.cjs` | 20 / 20 | **20 / 20** | ✔ |
| 出图 | `node tools/bf/shots/panel.js` | 6 张 | **6 张**（含新图） | ✔ |

断言量（脚本统计）：`tests` 的 `assert.` 调用 **991 → 1076（净 +85）**，用例 **83 → 86**；
`headless` 的 `A()` 断言 **399 → 420（净 +21）** —— **没有任何断言被删除**。

真 Chrome 那一轮需要本地 HTTP 服务：本会话开始时 `127.0.0.1:8000` **是关着的**（TCP 拒连），
我以**受管后台任务**起了一个 `python -m http.server 8000 --bind 127.0.0.1`（仓库根，已核验 `GET /breakfast.js → 200 / 221039 字节`），
跑完 e2e 后仍在运行；不需要时请直接关掉那个后台任务（job `pwsh-52`）。

---

## 7. 出图（含对比说明）

| 文件 | 说明 |
|---|---|
| **`测试截图/bf_plates_nocountdown.png`**（新，2794×1012） | **本轮的对比主图**。9 个专属盘**全部**有食物，盘龄 0.4 / 2 / 3.6 / 6 / 12 / 25 / 60 / 150 / 300s，画面上**全部**是「食材·热乎」+「∞ 可一直放着」。每列头顶标出盘龄与**旧口径下的下场**（0.4s→旧：热乎 14 分、2s→旧：温 10 分、3.6s→旧：凉 6 分、≥6s→**旧：早就糊了**）。底部两块并排：左边红框 = 本次删掉的旧文案（`4.1s / 2.3s / 0.9s 内送出` → 温 → 凉 → 糊了只能丢），右边绿框 = 现在真实画出来的文案。最后一行是**本帧实测计数**：「内送出」**0 处** · 「∞ 可一直放着」**9 处** · 「·热乎」**9 处**。 |
| `测试截图/bf_game.png`（更新，1770×1185） | 进行中整屏：5 盘（盘龄 0.4 / 2.2 / 3.6 / 30 / **90**s）全部「热乎」+「∞ 可一直放着」；顶栏明细改成「盘上 5/9 · 盘上永久保鲜（无倒计时）」；图例第 ④ 条与底排说明同步。**旧图在这里摆的是「热乎 / 温 / 凉 / 糊了」四态 + N.Ns 倒计时**。 |
| `测试截图/bf_columns.png`（更新） | 列对齐特写；底部注解从「盘上停留超过 4.5s 会糊，糊了只能双击丢掉」改成「盘上永久保鲜：没有倒计时 / 不会凉 / 不会糊，放多久都能上」（配色由橙改绿）。 |

出图**不是浏览器截图**：是把 `breakfast.js` 真发出的 Canvas2D 指令在 Node 里软件光栅化，
中文由 `tools/lib/text-compose.ps1` 用系统字体（微软雅黑）合成（本轮合成 291 段文字）。

---

## 8. 未完成项 / 诚实说明

1. **折中 ①②都没实现**（按要求只报告）。当前口径 = 用户原话的字面口径（零惩罚）。
2. **难度确实下降了**，而且**下降幅度比「取消倒计时」这句话听起来更大**：§3 的 A 组实测说明
   「锅内忘取就糊」在默认玩法里改动**之前**就触发不到，所以盘上那条链是唯一真实压力；
   去掉之后，默认玩法下「做好的早餐会报废」这件事**整体消失**。如果要补回压力，请从 §4 二选一。
3. **原版文件没有单独留档**（过程瑕疵，如实报告）：
   `tools/dev/patch-literal.js` 的默认策略是「每次改前刷新备份」，而我分两轮改了 `breakfast.js`
   （第二轮只改了 2 处文案/字段），于是 `breakfast.js.bf8bak`（221000 字节）现在等于**第一轮改完**那一版，
   **不是**改动前的 216155 字节原版；`tools/bf/shots/panel.js.bf8bak` 同理（= 第一轮改完）。
   * 可用的回滚点：`tools/bf/_patch/bf13_snapshot_after_patch2.js`（= 本次验收通过的 `breakfast.js` 快照）；
     第二轮改动的**双向**字面锚都在 `tools/bf/_patch/jobs_bf13b.json` 里，可反向应用。
   * 第一轮 24 个 job 的 `from`/`to` 锚与替换文本都在 `jobs_bf13.json` + `bf13_*.txt` 里，
     但**区间中间被删掉的原行**没有逐字留档 → 精确还原原版需要按这些锚重建，不能一键回退。
   * `tests/breakfast.test.cjs.bf8bak`（185548）与 `tools/bf/headless.js.bf8bak`（128990）**是改前原版**（只改过一轮）。
4. `SERVE_WINDOW / SERVE_WARN_SEC / openServeWindow / expireServeWindows / isServeWindowOpen / takeReady`
   作为**兼容层原样保留**（9 列都有盘 → 锅内窗口这条路径恒不触发），没有删除；`heatTierOf` 曲线也保留。
5. 没有跑 `run-all-tests.ps1` 全量（它包含麻将 / 格斗 / nlroute 等与本改动无关的套件）；
   只跑了与本改动相关且任务点名的 5 项 + `tests/audio-paths.test.cjs`。
   本会话期间 `mahjong.js` / `index.html` / `nlroute.js` 等文件正在被其他人改动，我一个字都没碰。
6. `debug.burnPlate()` 的语义变了：从「把盘拨到过期」改成「锅里先烧糊 → 再端上盘」——
   因为盘上已经不存在「自然变糊」这条路，糊盘现在只有这一个真实来源。
7. 验收用到的探针脚本（都留在 `tools/bf/_patch/`，不参与运行时）：
   `_bf13_preflight.js`（锚点预检）· `_bf13_pressure_probe.js`（§3 的压力点实测）。

---

### 附：本轮改动的逐字替换工件（审计用）
`tools/bf/_patch/jobs_bf13.json`（breakfast.js 第一轮 24 job）· `jobs_bf13b.json`（第二轮 2 job）·
`jobs_bf13_tests.json`（测试 9 job）· `jobs_bf13_headless.json`（无头 10 job）·
`jobs_bf13_panel.json` + `jobs_bf13_panel2.json`（出图 3 + 4 job）· 对应的 `bf13_*.txt` 替换文本。
全部经 `tools/dev/patch-literal.js` 落盘（默认刷新备份 + `node --check` 语法闸）。
