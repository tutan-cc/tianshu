# 实拍素材：授权未核实，不入库（请自行放置到本地）
#
# ⚠ 注意这里**只排除 video/，不再排除 audio/**。两者授权性质相反：
#     video/  = 实拍素材（68 个，授权未核实）→ 不入库 → 走带外分发包
#     audio/  = **全部自产**（305 个，StepAudio TTS/音效/BGM，可自由使用）→ **必须入库**
#
#   原先两者都排除，后果是同事 clone 下来**一条音频都没有**（308 个自产音频
#   全在带外包里）—— 自己用 AI/API 生成的素材本就该直接进仓库。
#
#   技术上还有个坑：仓库里的 video/ 是 junction（指向仓库外的媒体素材库），
#   而 **git 会透过 junction 提交里面的文件** —— 所以同一个 junction 上
#   做不到「自产入库、第三方不入库」。audio/ 因此改成了仓库里的真目录。
video/*
# 程序化动画为原创，入库
!video/fx_market.mp4
!video/fx_flood.mp4
!video/poster/fx_market.jpg
!video/poster/fx_flood.jpg

# 运行/构建产物与临时文件（不入库）
_prof*/
_temp/
_check.js
# 构建与测试产物（不入库）
#   dist/ 装三样东西：素材分发包（体积大，私下复制给协作者，不上 GitHub Release）
#   + 测试结果 JSON（带时间戳，每次跑测试都会改写 —— 落在这里就不会污染 git status）
#   + 分发说明 README（**入库**：它是协作流程的唯一说明）
#
# ⚠ 注意 git 的规则：**父目录被排除后，`!` 无法重新包含它里面的文件**。
#   所以不能写 `dist/` 再 `!dist/素材分发/README.md` —— 那样 README 仍被忽略（实测确认）。
#   正确做法：按「内容类型」上锁（大文件与产物），而不是锁整个 dist/。
dist/test-results/
dist/media-*.zip
dist/*.zip
dist/**/*.zip
dist/**/*.sha256
# 兜底：其它未知的大产物也别进库（README/manifest 之类小文本不受影响）
dist/**/*.png
dist/**/*.mp4
# 兜底：万一有脚本仍把结果写进 tests/，也不要进库（tests/ 只应放测试源码）
tests/*-results.json
tests/*_text.json
# 本地验证脚本（CDP 驱动，非游戏本体；归位后在 tools/verify/）
tools/verify/
# 出图流水线的 PNG 产物：由脚本重新生成，不入库（与 dist/test-results/ 同理）
测试截图/
*.bak
*.bak2
*.bak3
*.bf2bak
*.bf3bak
*.bf4bak
*.bf5bak
*.bf7bak
*.bf8bak
*.bf8bak0
*.mj2bak
*.mj3bak
*.mjbak
*.pickbak
*.fixbak
*.visbak
*.v14bak
*.v16bak
*.w35bak
*.bfbak
*.hintbak
*.layoutbak
*.log
_mj_render_out.txt

# 子代理调试中间产物（第三轮清理后追加，防止再次入库）
_bf_new_*.txt
_bf_jobs_*.json
_mj_*.txt
_probe_out/
_lovart_probe/
# mshta 探针的临时 .hta（正常情况下脚本跑完自己删；探针崩掉时会残留，兜底忽略）
_*.hta

# 编辑器/系统
.vscode/
.idea/
Thumbs.db
desktop.ini

# Python 编译产物（tools/audio/ 是 .py 工具链，跑一次就会生成）
__pycache__/
*.pyc

# 音频素材目录里的下划线开头文件：生成脚本的草稿/失败日志
#
# ⚠⚠ 这里**必须逐个精确写，绝不能用 `audio/**/_*` 这类通配** ——
#   曾经用过通配，后果是**把测试依赖的合法目录一起挡在仓库外**：
#     `audio/bf/_unused/` 是早餐店欢呼语音的归档目录（78 个旧件，试听页与
#     tests/breakfast.test.cjs 都要用，测试断言 archived.length >= 70）。
#     通配把它整目录忽略 → 文件进不了仓库 → 换台机器 clone 后测试必失败，
#     而作者本机因为文件还在（未跟踪）测试照样通过，**故障只在别人机器上出现**。
#   所以：只列真正要拦的中间产物名，不要用通配。
#   反面清单：以下这些看着像"中间产物"但其实是**正式素材**，不能拦 ——
#     audio/bf/_unused/    早餐店欢呼语音归档（测试与试听页依赖）
#   新增规则前先问一句：这个路径会不会被测试或代码引用？
#
# 实际情况：本仓库的加工中间产物一律放在**仓库外**的 audio-工作区\，
# 本来就不必在这里拦。下面几条只是防手滑。
audio/**/_failures.json
audio/**/_seat*_src/
audio/**/_sfx_src/
audio/**/_amb_src/
audio/**/_bgm_src/
audio/**/_mj0_src/
