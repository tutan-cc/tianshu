# 协作者上手指南

> 这份文档写给**拿到代码仓库、想把游戏完整跑起来**的人。
> 只跑代码不接素材也能启动，但过场是黑底渐变 + 字幕；接上素材才有实拍画面与配音。

---

## 为什么部分素材要单独发

**代码与「第三方素材」是分开流转的，这不是偷懒，是必须的。**
但注意：**只有授权未核实的实拍视频需要单独拿，音频和图片都已在仓库里**。

| 目录 | 授权 | 是否入库 | 大小 |
|---|---|---|---|
| `audio/` 配音 · 音效 · BGM | 项目自产（AI 生成） | ✅ **已入库，clone 就有** | 12 MB |
| `art/` 图片素材 | 项目自产（AI 生成） | ✅ **已入库，clone 就有** | — |
| `video/` 实拍视频 + 剧照 | **未核实** | ❌ 单独发 | 208 MB |

视频之所以必须单独发：

1. **版权原因**：实拍素材的授权状态未核实，`SOURCES.md` 里明确写了不入库
2. **体积**：208 MB，单文件超 100 MiB 会被 GitHub 直接拒收
3. **git 的特性**：素材一旦提交就**永久留在历史里**，删都删不掉。素材要迭代（重拍、换 AI 生成），
   若每次都进 git，几轮下来仓库就会膨胀到无法 clone
4. **为什么也不走 GitHub Release**：Release 附件虽然是公开可下载的，但正因为公开 ——
   传上去等同于把这 68 个授权未核实的第三方素材**公开发布**，与「仅供本地试验」冲突。
   所以素材由维护者**私下发给你**（详见 `dist/素材分发/README.md`）

> 自产素材（`audio/` `art/`）**不适用**上面任何一条：授权清晰、体积小、且不会反复重做 ——
> 所以它们直接入库。原先音频也走带外分发，结果是同事 clone 下来一条音频都没有，已修正。

---

## 三步跑起来

### 第 1 步 · clone 代码

```bash
git clone https://github.com/tutan-cc/tianshu.git
cd tianshu
```

> 本项目仓库原为 `tutan-cc/-`，已改名为 **`tutan-cc/tianshu`**（旧地址会自动重定向）。
> ⚠️ 别跟同名的校园项目 `tutan123/tianshu` 搞混 —— 两个仓库都叫 `tianshu`，
> 只靠 owner 区分（`tutan-cc` 是本项目）。引用时请带 owner 全写。

### 第 2 步 · 拿到视频素材包并解压

> **先明确一件事：音频不需要你额外操作。**
>
> | 目录 | 来源 | 大小 |
> |---|---|---|
> | `audio/` 配音 · 音效 · BGM | **仓库自带**（`git clone` 就有） | 12 MB |
> | `art/` 图片素材 | **仓库自带** | — |
> | `video/` 实拍视频 | **要额外拿素材包** | 208 MB |
>
> 音频全是本项目用 AI 生成的（305 个文件），可自由使用，所以直接入库了。
> 只有实拍视频因为授权未核实，走带外分发。

视频素材**不在 GitHub 上**（授权未核实 + 体积大），由项目维护者**直接发给你**
（网盘 / 移动硬盘 / 局域网，随包会附一个 `.sha256` 校验文件）。

拿到 `media-v2.0.zip`（约 208 MB，文件名里的版本号可能更新）后，可选先核对完整性：

```powershell
Get-FileHash .\media-v2.0.zip -Algorithm SHA256
# 与 media-v2.0.zip.sha256 里的值比对，一致再解压
```

然后**解压到仓库的同级目录**，目录名用 `Tianshu-Prototype-媒体素材`：

```
某目录\
├─ <仓库目录>\                        ← git clone 出来的（audio\ art\ 已经在这里面）
└─ Tianshu-Prototype-媒体素材\         ← 视频素材包解压出来的
   └─ video\                          35 个 mp4
      └─ poster\                      35 张剧照
```

> 素材包里**只有 video\**。里面也有一份 `audio\` 的话不用管 ——
> 仓库里那份才是权威，脚本会以仓库的为准。
>
> 目录名不是硬性要求（脚本会自动扫描同级目录里的素材库），但用 `Tianshu-Prototype-媒体素材` 会被优先命中。

### 第 3 步 · 对接素材

在仓库目录里跑一次：

```powershell
powershell -ExecutionPolicy Bypass -File .\link-media.ps1
```

脚本会：

1. 自动找到素材库
2. 把素材库的 `video\` 以**目录联接（junction）**挂到仓库根下
   —— `audio\` **不联接**：它是仓库自带的真目录（305 个自产音频），脚本会明确报告这一点
3. 拿 `媒体清单.json` 对全部 375 个文件做 **SHA256 逐文件校验**，缺哪个直接列出来

看到下面这样就成功了：

```
清单 v2.0 · 视频 35 · 剧照 35 · 音频 305（共 375）
video\ 已联接（正确），跳过
audio\ 为仓库自带真目录（305 个文件），不联接
校验通过 375 / 375
素材已接通。起服务：python -m http.server 8000  然后开 http://127.0.0.1:8000/index.html
```

### 起服务

```bash
python -m http.server 8000
```

然后浏览器打开 **http://127.0.0.1:8000/index.html**（推荐 Chrome）。

> 双击 `index.html` 也能玩，但浏览器对本地视频有限制，用本地服务器最稳。

### 可选 · 自然语言影子模式（要 TypeSafe key，不起也不影响玩）

侧栏那个「🧠 自然语言 · 影子模式」输入框要**本地反代**转发才用得了（TypeSafe 接口不放行浏览器直连，且这样 key 不用进浏览器）：
设一次环境变量 `setx TYPESAFE_API_KEY "apikey_…"`（新终端生效），然后一条命令起环境：`powershell -NoProfile -ExecutionPolicy Bypass -File tools/dev/nlroute-up.ps1` ——
它会同时起反代（127.0.0.1:8010）与静态服务（127.0.0.1:8000）并打印下一步；收摊用同一条命令加 `-Stop`。**它只记录判断结果、绝不自动跳转**，玩家真实去向记在 `localStorage['cs2_nl_log']`，用侧栏「📤 导出语料」导出。

### 起服务后先确认「跑的是最新代码」：右下角版本戳

页面**右下角**有一枚常驻水印（标题屏和游戏内都有，不可点击、不挡操作）：

```
天枢原型 · build 20260920-1738 · ch 84020f · bf 186K · mj 288K · idx 225K · map 68K
```

- `build` = `mahjong.js` / `breakfast.js` / `map3d.js` 里**最新的修改时间**
  （不是 `index.html` 的：它的 mtime 就是"上次盖章时间"，纳进来 build 会每盖一次往前走一格）
- `ch` = 内容指纹，**任一文件变了必变**；后面的 `186K / 288K …` 是各模块体积

> ⚠ `build` 取的是**文件系统 mtime**，所以 clone / 解压 / 检出会把三个模块刷成"落地时间"，
> 它衡量的是"文件什么时候落到你机器上"，不完全是"代码什么时候写的"。
> 要判断内容有没有变，看 `ch` 指纹（`--check` 也是以它为准）。

**怎么用它判断缓存**（本项目真实踩过的坑：CSS/图片是新的，但 `breakfast.js` 走了缓存，
新功能不生效却看不出来）：

| 现象 | 结论 | 怎么办 |
|---|---|---|
| `build` **晚于或等于**你刚拉取代码的时间 | 跑的是新代码 | 正常玩 |
| `build` **早于**你刚拉取的代码 | **浏览器缓存** | **Ctrl+Shift+R** 强刷，或用无痕窗口 |

控制台还可以一行自查：`__BUILD.fingerprint` / `__BUILD.build` / `__BUILD.sizes`。

如果你自己改了代码，改完重新盖章（在仓库根目录）：

```bash
node tools/dev/stamp.js            # 重新盖章（写进 index.html，同时打到控制台）
node tools/dev/stamp.js --check    # 校验：页面戳 ≠ 当前文件 → 警告 + 退出码 1
```

`--check` 的退出码：`0` = 一致；`1` = 不一致（说明页面上的戳过期，该重新跑 stamp）；
`2` = 用法错误或文件缺失。所以它可以直接当**打包前 / CI 的闸门**。
脚本只改 `<!-- BUILD-STAMP:BEGIN -->` 与 `<!-- BUILD-STAMP:END -->` 之间的内容，
逐字替换、**幂等**（重复跑不会多插一块）；没有标记块时会自动插一次到 `</body>` 前。

---

## 常见问题

**Q：我明明拉了最新代码，但新功能没生效 / 看起来还是旧界面？**

先看页面**右下角的版本戳**。若上面的 `build` 时间**早于**你拉取代码的时间，就是
**浏览器缓存**了 JS/CSS —— **Ctrl+Shift+R** 强刷，或用无痕窗口再开一次。
仍不对就在控制台敲 `__BUILD`，跟本地 `node tools/dev/stamp.js --print` 的输出比对指纹。

**Q：改了代码后右下角的戳还是旧时间？**

戳不会自己更新 —— 跑一次 `node tools/dev/stamp.js` 重新盖章即可。
打包/交付前建议跑 `node tools/dev/stamp.js --check`（不一致会以退出码 1 失败）。

**Q：视频为什么不直接把素材 copy 进 `video\`？**

可以，但没必要。目录联接**零拷贝、不占额外空间**，而且对 git 不可见（`git status` 保持干净），
`git clean -fdx` 也不会误删你的素材。同理，以后换素材只需替换素材库里的文件。

**Q：那 `audio\` 为什么反而入库了？**

因为授权性质不同：`audio\` 全是本项目用 AI 生成的（可自由使用），
`video\` 是授权未核实的实拍素材。

还有一个技术原因：**git 会透过 junction 提交里面的文件**，所以同一个 junction 上
做不到「自产入库、第三方不入库」。`audio\` 因此改成真目录直接入库 ——
否则你 clone 下来会**一条音频都没有**（这是修过的真实问题）。

**Q：校验报「缺失」怎么办？**

素材包没下全。重新下载 `media-vX.XX.zip` 并**完整解压**（别只拖其中几个文件出来）。
若反复失败，检查下载是否被中断。

**Q：我改了素材，校验报「内容不符」？**

正常 —— 清单记的是原始素材的 SHA256。你自己换过文件后，重跑 `.\pack-media.ps1`
会重新生成清单，把新素材的哈希写进去。

**Q：素材库能放别处吗（不想放同级目录）？**

能：

```powershell
powershell -ExecutionPolicy Bypass -File .\link-media.ps1 -MediaDir "D:\anywhere\media"
```

**Q：没有素材能跑吗？**

能。游戏有三级画面兜底（视频 → 剧照 → 动态渐变），启动时会弹自检面板说明缺什么，
剧情流程照样能通关，只是没有实拍画面和配音。

---

## ⚠️ 素材授权（重要）

`媒体清单.json` 里每个文件都带 `source` 与 `license` 字段：

| source | 含义 |
|---|---|
| `original` | 原创程序化生成，可自由入库（`fx_market.mp4` / `fx_flood.mp4` 及其剧照） |
| `thirdparty` | 实拍素材，**授权未核实，仅供本地试验** |

**不要把 `thirdparty` 素材提交进任何公开仓库，也不要随代码分发。**
项目计划是用自拍与 AI 生成内容逐步替换它们 —— 替换后重跑 `pack-media.ps1`
更新清单即可，代码与目录结构都不用动。

---

## 开发者：素材有更新时怎么出新的分发包

```powershell
# 1. 更新素材库内容后，重新生成清单 + 打包
#    脚本会：算清单 → 打包 → 放进 dist\素材分发\ → 生成 .sha256 校验文件
powershell -ExecutionPolicy Bypass -File .\pack-media.ps1 -Version v2.1

# 2. 把改动过的 媒体清单.json 提交进 git（这是协作者校验素材的依据）
git add 媒体清单.json && git commit -m "素材更新 v2.1"

# 3. 把 dist\素材分发\media-v2.1.zip（和它的 .sha256）复制给协作者
#    网盘 / 移动硬盘 / 局域网都行 —— 注意别放到公开可下载的位置

# 4. 通知协作者：解压覆盖旧素材库，再跑一次 link-media.ps1（音频无需操作，已在仓库里）
```

> 为什么不用 GitHub Release：Release 附件是公开可下载的，而包内的 68 个第三方视频素材
> 授权未核实。等换成自拍 / AI 生成内容后，再考虑走 Release 公开发布。
